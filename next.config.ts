import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'id.pinterest.com', "d3njjcbhbojbot.cloudfront.net", 'www.shutterstock.com',
      "d15cw65ipctsrr.cloudfront.net", 'source.unsplash.com', 's3.amazonaws.com', 'www.pexels.com'],
  },
};

export default nextConfig;
